# 用户记忆（对话上下文）

## 项目背景

- 华中科技大学计算机科学与技术学院课程作业
- 基于模板项目 `YA_MCPServer_Template` 开发自己的 MCP Server
- 目标：学习 MCP 协议，掌握扩展 LLM 能力的工程方法

## 选题决策

- 从 11-47 号中等难度选题中选择
- **最终倾向：21 个性化知识管理智能体**
- 理由：边界清晰、三层结构自然、技术栈纯 Python、AI 可以直接写大部分代码

## 技术方向

- 存储：SQLite + ChromaDB（向量检索）
- 三层结构规划：
  - Tools：add_note / search_notes / update_note / delete_note / summarize_topic
  - Resources：knowledge://notes / knowledge://tags
  - Prompts：daily_review / knowledge_gap / connect_ideas

## 环境要求（第2张）

- Git
- uv（Python 包与环境管理，测试时用 uv 创建虚拟环境并安装依赖）
- node.js（用于 MCP Inspector 调试，**必须用 MCP Inspector 确认功能无异常**）
- sops（可选，用于加密敏感信息，Age 非对称加密）

## 项目结构规范（第3张）

| 目录/文件 | 功能 | 规范 |
|-----------|------|------|
| docs/ | 安装工具环境的教程、如何开发调试 MCP Server | - |
| core/ | 核心业务逻辑 | 功能模块化、支持异步、异常处理 |
| tools/ | MCP Tool 实现 | 可供 MCP Client 调用的工具 |
| resources/ | MCP Resources 实现 | 可供 MCP Client 调用的资源 |
| prompts/ | MCP Prompts 实现 | 可供 MCP Client 调用的指令 |
| config.yaml | 配置 & 元数据 | YAML 格式、只存储数据 |
| server.py | MCP Server 启动 & 调度 | **无需修改** |
| setup.py | 在 setup 函数中预先准备其他依赖，如下载模型文件、数据集 | **重点修改入口** |
| modules/YA_Common | 通用工具函数如日志、错误处理 | **无需修改** |
| modules/YA_Secrets | 管理敏感的环境变量和密钥 | 如果设计 API 密钥等请使用该模块统一管理 |

## 开发流程规范（第5张）

### 配置文件同步（三处要保持一致）
- `config.yaml`：修改服务器名称、描述等元数据
- `pyproject.toml`：name、version、description 与 config.yaml 一致
- `README.md`：项目名称与描述保持一致
- 命名示例：`YA_MCPServer_XXXXX`

### Git 工作流（默认分支必须是 main）
1. `git init`
2. `git add .` + `git commit -m "Initial Commit"`
3. `git checkout -b dev main`（在 dev 分支开发）
4. 开发完成：`git checkout main` + `git merge dev`

## 开发运行方式（第6张）

### 虚拟环境
- 创建并安装依赖：`uv sync`
- 激活（Windows）：`.venv\Scripts\activate`
- 激活（Linux/macOS）：`source .venv/bin/activate`

### 运行服务器
- `uv run server.py`
- 默认 SSE 模式，地址 `127.0.0.1:12345`
- 改 `config.yaml` 的 `transport.type` 为 `stdio` 可切换模式

### 模板已有示例
- `hello_tool`：获取服务器配置信息
- `hello_resource`：返回 README.md、日志文件
- `hello_prompt`：生成问候消息
- `hello_secrets`：演示 SOPS 加密密钥用法

## 服务器启动效果（第7张）

- 命令：`uv run .\server.py`
- 启动日志顺序：setup → 注册 tools → 注册 prompts → 注册 resources → start
- 成功后显示 ASCII banner，然后 Uvicorn 监听 `http://127.0.0.1:12345`

## 代码规范（第8张）

- 必须用 `@YA_MCPServer_Tool()` / `@YA_MCPServer_Resource()` / `@YA_MCPServer_Prompt()` 装饰器注册
- 每个类/函数必须有详细 docstring（说明功能、Args、Returns）
- 每个函数必须有异常处理（try/except），防止服务器异常退出

## 调试方法（第9张）

### MCP Inspector 调试
- Stdio 模式：`mcp dev server.py` → Inspector 页面选 transport=stdio → Connect（无需手动启动服务端）
- SSE 模式：先启动服务端 → 另一终端 `mcp dev server.py` → Inspector 选 transport=sse，输入 `http://127.0.0.1:12345` → Connect

### 其他调试方法
- Python 实现 MCP Client
- VSCode、Claude Desktop 等支持 MCP 协议的客户端

## MCP Inspector 调试界面（第10张）

- 这是 MCP Inspector v0.18.0 连接成功后的样子
- Transport Type: SSE，URL: `http://127.0.0.1:12345`，Connection Type: Direct
- 左侧显示服务器名称 `YA_MCPServer_Template` Version 1.25.0
- 顶部 tab：Resources / Prompts / Tools / Ping / Sampling / Elicitations / Roots / Auth / Metadata
- Tools 列表中可以看到 `get_server_config` 和 `greeting_tool`
- 右侧可以填参数并调用工具，查看 Output Schema
- History 区域记录调用历史：initialize → tools/list → tools/call

## 提交要求（第11张）

### 最佳实践（红色重点）
1. 使用远程 Git 托管平台（如 GitHub）管理项目
2. 开发完成后，重新克隆仓库到本地，重新创建虚拟环境，重新运行 MCP Server
3. **确保在只具备基本环境的机器上，运行你的 MCP Server 没有任何问题**

### 提交内容（右侧文件结构）
提交的仓库应包含标准模板结构：core / docs / modules / prompts / resources / tools / .gitignore / .gitmodules / .python-version / .sops.yaml / config.yaml / env.yaml / pyproject.toml / README.md / server.py / setup.py / uv.lock

## 提交须知（第12张）

### 打包要求
- 打包为 zip（禁止 7z 等其他格式）
- 禁止包含 `.venv`、`logs` 等非核心代码文件
- 禁止修改 `modules`、`server.py` 等文件中的代码

### README 必须包含
- 组员信息、Tools/Resources/Prompts 列表
- 项目结构（新增文件都实现了什么功能）
- 其他说明：sops 密钥变量用途、是否用了 PyTorch/TensorFlow、是否用了 ML/DL 模型

## 技术决策更新

- **sops 密钥管理**：确认不必要（纯本地知识管理系统，无外部 API 调用需求）
- 可以跳过 sops 配置，专注核心 MCP 功能开发

## 当前进度

- 已收全部图片（第1-12张），需求已完整记录
- sops 必要性已确认（不需要）
- 可以开始写代码了
